{
  "name": "openflows-oauth-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker OAuth proxy for Decap CMS GitHub backend",
  "main": "src/index.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
